<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ultra.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".status.activity.ActionBarActivity"
            android:theme="@style/ActionBar" />
        <activity android:name=".network.NetWorkActivity" />
        <activity android:name=".status.activity.WebActivity" />
        <activity android:name=".status.activity.ShapeActivity" />
        <activity android:name=".status.activity.ColorActivity" />
        <activity android:name=".status.activity.FlyMeActivity" />
        <activity android:name=".status.activity.StatusActivity" />
        <activity android:name=".status.activity.FragmentActivity" />
        <activity android:name=".status.activity.PictureActivity" />
        <activity android:name=".status.activity.ImgAndColorActivity" />
        <activity android:name=".status.activity.CoordinatorActivity" />
        <activity android:name=".status.activity.WhiteStatusBarActivity" />
    </application>

</manifest>